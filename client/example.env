# React App Configuration
REACT_APP_API_URL=http://localhost:5001/api

# Add any other client-side environment variables here
# Note: Only variables prefixed with REACT_APP_ are accessible in React

# Source Maps Configuration
# SECURITY: Set to false in production to prevent exposing source code
# In development, source maps are helpful for debugging
# GENERATE_SOURCEMAP=true  # Default for development
# For production, this should always be false (see .env.production)
