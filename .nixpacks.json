{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_18"]
    },
    "install": {
      "cmds": ["cd server && npm install --only=production"]
    },
    "build": {
      "cmds": ["echo 'Backend has no build step'"]
    }
  },
  "start": {
    "cmd": "cd server && npm start"
  }
}

