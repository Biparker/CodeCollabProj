## Feature 10 Complete: Migrate client hooks - projects domain

### Summary
Successfully migrated all project-related hooks from JavaScript to TypeScript.

### Files Migrated
1. **useProjects.ts** - List projects with filters, search, user projects, status, and featured projects
   - Added proper TypeScript types for `UseQueryOptions` and `UseQueryResult`
   - Typed `ProjectFilters` parameter using existing service types
   - Typed options parameter to exclude queryKey/queryFn for proper composition

2. **useProject.ts** - Single project fetch by ID
   - Added custom `QueryErrorWithStatus` interface for retry logic
   - Typed the retry function properly with error status handling

3. **useProjectMutations.ts** - CRUD operations (create/update/delete)
   - Added `UpdateMutationContext` and `DeleteMutationContext` interfaces for optimistic updates
   - Properly typed all mutation callbacks (onMutate, onError, onSuccess, onSettled)
   - Used `UseMutationResult` for return types

4. **useCollaborationMutations.ts** - Collaboration operations (join/leave/request/handle)
   - Imported and used service response types (`JoinLeaveResponse`, `CollaborationResponse`)
   - Used `CollaborationRequestPayload` from service for handle request mutation

5. **index.ts** - Central export file
   - Updated to use `.ts` extension implicitly for TypeScript module resolution
   - Exports all named hooks and default exports

### Types Used
- From `@tanstack/react-query`: `useQuery`, `useMutation`, `useQueryClient`, `UseQueryOptions`, `UseQueryResult`, `UseMutationResult`
- From `../../types`: `Project`, `ProjectStatus`, `ApiError`
- From `../../services/projectsService`: `ProjectFilters`, `ProjectUpdatePayload`, `DeleteProjectResponse`, `JoinLeaveResponse`, `CollaborationResponse`, `CollaborationRequestPayload`
- From `../../config/queryClient`: `queryKeys`, `invalidateQueries`

### Verification
- Ran `npx tsc --noEmit` - no errors in hooks/projects directory
- All files compile without TypeScript errors
- Changes already committed in prior commit aa9960d

### Notes
The work was already committed by another parallel worker (feature-12). The files I created match the committed versions exactly.
