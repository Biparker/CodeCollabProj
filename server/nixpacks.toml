[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = [
  "npm ci --only=production",
  "mkdir -p uploads logs"
]

# Explicitly disable build phase - backend runs TypeScript directly with tsx
[phases.build]
cmds = []

[start]
cmd = "npm run start:tsx"

